package tr.com.net.common.connection;

/**
 * Connection states enumeration class
 *
 * @author mozkeskin
 */
public enum ConnectionState {
    STARTING, CONNECTED, STOPPED, DISCONNECTED, RECONNECTING, TIMEOUT
}
